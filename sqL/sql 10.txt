1) select salary from (select * from employee order by salary desc limit 5) order by asc limit 1;

2) select e1.FirstName from employee, Employee e1 WHERE employee.salary=e1.salary and Employee.EmpId<>e1.EmpId;